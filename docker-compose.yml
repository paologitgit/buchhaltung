services:
  gigfinder:
    build: .
    ports:
      - "8090:8090"
    env_file:
      - .env
    volumes:
      # ganzes data/-Verzeichnis: Suchergebnisse + Adressbuch-Datenbank
      - ./data:/app/data
    restart: unless-stopped
